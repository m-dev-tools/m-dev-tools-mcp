{
  "$schema": "https://raw.githubusercontent.com/m-dev-tools/.github/main/profile/repo.meta.schema.json",
  "kind": "m-dev-tools-mcp.tools",
  "package": "m_dev_tools_mcp",
  "version": "0.1.0",
  "tool_count": 3,
  "tools": [
    {
      "name": "describe",
      "summary": "Return a pointer-blob for a typed ID.",
      "description": "Return a pointer-blob for a typed ID.\n\n        Supported kinds: ``tool:`` / ``module:`` / ``cmd:`` / ``recipe:``.\n        Does not inline payloads \u2014 returns URLs the caller should\n        fetch next, keeping the catalog's \"pointers, not facts\"\n        invariant.",
      "parameters": {
        "type": "object",
        "properties": {
          "typed_id": {
            "type": "string",
            "python_annotation": "str"
          }
        },
        "required": [
          "typed_id"
        ]
      },
      "return_annotation": "dict[str, Any]"
    },
    {
      "name": "route_intent",
      "summary": "Return typed IDs matching the plain-English intent.",
      "description": "Return typed IDs matching the plain-English intent.\n\n        Example: ``route_intent(\"parse JSON in M\")`` returns\n        ``[\"module:m-stdlib#STDJSON\"]``. Results are ``[primary,\n        *see_also]`` from the matched task_index row.",
      "parameters": {
        "type": "object",
        "properties": {
          "query": {
            "type": "string",
            "python_annotation": "str"
          }
        },
        "required": [
          "query"
        ]
      },
      "return_annotation": "list[str]"
    },
    {
      "name": "verify",
      "summary": "List the ``verification_commands`` declared in a repo's",
      "description": "List the ``verification_commands`` declared in a repo's\n        ``repo.meta.json``.\n\n        Accepts either a bare repo slug (``m-cli``) or a typed ID\n        (``tool:m-cli``). Does NOT execute the commands.",
      "parameters": {
        "type": "object",
        "properties": {
          "repo": {
            "type": "string",
            "python_annotation": "str"
          }
        },
        "required": [
          "repo"
        ]
      },
      "return_annotation": "list[str]"
    }
  ]
}
